# Vite environment variables
# Copy to .env.local or export in shell to override defaults

# Base URL of the Spring Boot backend
# The app fetches ${VITE_API_URL}/api/concept-map
VITE_API_URL=http://127.0.0.1:8080
